 <h2>New Gallery for <%= @user.name %></h2>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for galleries</p>
     <ul class='gallery-guidelines'>
       <li>Post only pictures with <%= @user.name %> in them.</li>
       <li>Keep it PG. This is a family site.</li>
       <li>Thanks for sharing <%= @user.name %> with the community!</li>
     </ul>
   </div>
   <div class="col-md-8">
      <%= render partial: 'form', locals: { user: @user, gallery: @gallery } %>
   </div>
 </div>
