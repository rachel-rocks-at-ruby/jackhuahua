  <% unless @dog == current_user %>
    <% if @friend_if %>
      <%= link_to "Unfriend dog", "#{}",onclick: "alert('I dont do anything')", method: :delete, class: 'btn btn-danger'  %>
    <% else %>
      <%= link_to "Friend dog", friendships_path(friend_id: @dog.id), method: :post, class: 'btn btn-primary'%>
    <% end %>
  <% end %>